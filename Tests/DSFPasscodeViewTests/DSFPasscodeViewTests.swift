    import XCTest
    @testable import DSFPasscodeView

    final class DSFPasscodeViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DSFPasscodeView().text, "Hello, World!")
        }
    }
